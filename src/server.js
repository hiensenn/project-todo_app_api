
const tarefas = {
    'fazer' : 'exercício',
}

const usuarios = {
      'nome': 'Hiensen'
}
























// const express = require('express')
// const app = express()
// const port = 3022


// app.get('/usuario', (req, res) => {
//     res.send(`rota ativada com GET e recurso p/ ${usuario.nome}  ${usuario.sobrenome} senha  ${usuario.password}`)
//   })

// app.get('/tarefa', (req, res) => {
//     res.send(`rota ativada com GET e recurso tarefas :${tarefa.fazer}`)
//   })

// app.listen(port, () => {
//   console.log(`Example app listening at http://localhost:${port}`)
// })

